{
  "scripts": {
    "prettier": "prettier '**/{*.{js?(on),ts?(x),graphql,md,scss},.*.js?(on)}' --write --list-different --config prettier.config.js",
    "prettier-check": "yarn -s run prettier --write=false"
  },
  "devDependencies": {
    "@khulnasoft/prettierrc": "3.0.3",
    "prettier": "1.19.1"
  }
}
